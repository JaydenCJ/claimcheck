# Everything routes through the internal registry manager, and the @kestrel
# scope is pinned explicitly so it never depends on the ambient default.
registry=https://npm.kestrel.test/
@kestrel:registry=https://npm.kestrel.test/
