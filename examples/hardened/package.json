{
  "name": "@kestrel/api",
  "version": "3.2.0",
  "description": "Example: the same service with its resolver configs hardened",
  "private": true,
  "dependencies": {
    "http-kit": "^2.1.0",
    "@kestrel/auth": "^1.9.1",
    "@kestrel/metrics": "^2.4.0",
    "@kestrel/tracing": "^0.8.0"
  }
}
